'use strict';
export var diffMode : boolean = false;
export var compareMode : boolean = false;
export function setDiffMode(flag: boolean) {
    diffMode = flag;
}
export function setCompareMode(flag: boolean) {
    compareMode = flag;
}